const express = require('express');
const router = express.Router();

// Note: Site settings are handled by the dedicated siteSettings.js route file
// This prevents route conflicts and ensures consistent handling

module.exports = router;